# 비즈니스 규칙 정의서

| 항목 | 내용 |
|:---|:---|
| 사업명 | my-stock |
| 작성일 | 2026-06-07 |
| 버전 | v0.1 |
| 기준 문서 | 요구사항정의서_my-stock_20260606.md · CLAUDE.md |

---

## 1. 비즈니스 규칙 요약

| 도메인 | 규칙 수 | 높음 | 중간 | 낮음 |
|:---|:---:|:---:|:---:|:---:|
| 인증 | 4 | 3 | 1 | 0 |
| KIS API 제약 | 4 | 4 | 0 | 0 |
| 캐싱 | 4 | 2 | 2 | 0 |
| 데이터 처리 | 4 | 2 | 1 | 1 |
| **합계** | **16** | **11** | **4** | **1** |

---

## 2. 비즈니스 규칙 목록

### 인증 도메인

#### BR-001 · 이메일 허용 검증

| 항목 | 내용 |
|:---|:---|
| **규칙명** | 이메일 허용 검증 |
| **도메인** | 인증 |
| **조건 (When/If)** | 환경변수 `ALLOWED_EMAIL`이 설정된 상태에서 Google OAuth 로그인 시도 시 |
| **행동 (Then)** | 로그인 계정 이메일이 `ALLOWED_EMAIL` 값과 일치하는 경우에만 세션 발급. 불일치 시 HTTP 401 반환 |
| **우선순위** | 높음 |
| **상태** | 확정 |
| **연관** | FR-001, SR-002 |

#### BR-002 · JWT 세션 전역 보호

| 항목 | 내용 |
|:---|:---|
| **규칙명** | JWT 세션 전역 보호 |
| **도메인** | 인증 |
| **조건 (When/If)** | 사용자가 모든 페이지 또는 API 엔드포인트에 접근 시도 시 |
| **행동 (Then)** | `middleware.ts`에서 NextAuth JWT 세션 유효성 검증. 미인증 요청은 `/auth/signin`으로 리다이렉트 |
| **우선순위** | 높음 |
| **상태** | 확정 |
| **연관** | FR-001, SR-001 |

#### BR-003 · 세션 만료 UI 처리

| 항목 | 내용 |
|:---|:---|
| **규칙명** | 세션 만료 UI 처리 |
| **도메인** | 인증 |
| **조건 (When/If)** | 이미 로그인한 사용자의 요청에서 API가 HTTP 401을 반환 시 |
| **행동 (Then)** | 페이지 강제 리다이렉트 대신 현재 화면에 로그인 안내 UI 프롬프트 표시 |
| **우선순위** | 중간 |
| **상태** | 확정 |
| **연관** | SR-005 |

#### BR-004 · API 키 서버 전용 격리

| 항목 | 내용 |
|:---|:---|
| **규칙명** | API 키 서버 전용 격리 |
| **도메인** | 인증 |
| **조건 (When/If)** | 컴포넌트 코드 작성 및 빌드 시 |
| **행동 (Then)** | KIS·DART·OpenAI·Google 자격증명은 서버 사이드 환경변수로만 관리. 클라이언트 번들에 포함 절대 금지 |
| **우선순위** | 높음 |
| **상태** | 확정 |
| **연관** | SR-003 |

---

### KIS API 제약 도메인

#### BR-005 · API 호출 스로틀 준수

| 항목 | 내용 |
|:---|:---|
| **규칙명** | API 호출 스로틀 준수 |
| **도메인** | KIS API 제약 |
| **조건 (When/If)** | KIS Open API를 연속으로 호출 시 |
| **행동 (Then)** | 호출 간 최소 `KIS_THROTTLE_MS`(기본 400ms) 이상 대기. 미준수 시 EGW00133 오류 발생 |
| **우선순위** | 높음 |
| **상태** | 확정 |
| **연관** | CR-003, PR-004 |

#### BR-006 · 토큰 단일 발급 원칙

| 항목 | 내용 |
|:---|:---|
| **규칙명** | 토큰 단일 발급 원칙 |
| **도메인** | KIS API 제약 |
| **조건 (When/If)** | 신규 KIS 접근 토큰 발급 요청 발생 시 |
| **행동 (Then)** | 유효 토큰이 globalThis 또는 /tmp/ 캐시에 존재하면 재사용. 만료된 경우에만 신규 발급 (1일 1회 제한) |
| **우선순위** | 높음 |
| **상태** | 확정 |
| **연관** | CR-002 |

#### BR-007 · 토큰 발급 레이스 컨디션 방지

| 항목 | 내용 |
|:---|:---|
| **규칙명** | 토큰 발급 레이스 컨디션 방지 |
| **도메인** | KIS API 제약 |
| **조건 (When/If)** | 서버리스 인스턴스 여러 개가 동시에 토큰 발급을 시도 시 |
| **행동 (Then)** | `softExpireKisToken()`으로 토큰을 선점적으로 만료 처리하여 중복 발급 차단. 가장 빠른 인스턴스만 발급 진행 |
| **우선순위** | 높음 |
| **상태** | 확정 |

#### BR-008 · 토큰 다계층 캐시 조회

| 항목 | 내용 |
|:---|:---|
| **규칙명** | 토큰 다계층 캐시 조회 |
| **도메인** | KIS API 제약 |
| **조건 (When/If)** | KIS 접근 토큰이 필요한 API 호출 시 |
| **행동 (Then)** | `globalThis` → `/tmp/`(또는 `.next/cache/`) 순서로 조회. 없으면 발급 후 양쪽 모두 저장 |
| **우선순위** | 높음 |
| **상태** | 확정 |
| **연관** | DR-004 |

---

### 캐싱 도메인

#### BR-009 · KIS/DART 캐시 TTL 30분

| 항목 | 내용 |
|:---|:---|
| **규칙명** | KIS/DART 캐시 TTL 30분 |
| **도메인** | 캐싱 |
| **조건 (When/If)** | `_TICKER_CACHE_` 탭의 캐시 항목을 조회 시 |
| **행동 (Then)** | 저장 시각 기준 30분 초과 항목은 만료 처리하여 해당 API를 재호출, 결과를 갱신 |
| **우선순위** | 높음 |
| **상태** | 확정 |
| **연관** | DR-002 |

#### BR-010 · AI 분석 캐시 TTL 7일

| 항목 | 내용 |
|:---|:---|
| **규칙명** | AI 분석 캐시 TTL 7일 |
| **도메인** | 캐싱 |
| **조건 (When/If)** | `_AI_CACHE_` 탭의 AI 분석 캐시 항목을 조회 시 |
| **행동 (Then)** | 저장 시각 기준 7일 초과 항목은 만료 처리하여 OpenAI API 재호출 후 결과 갱신 |
| **우선순위** | 높음 |
| **상태** | 확정 |
| **연관** | DR-003 |

#### BR-011 · 캐시 키 형식 통일

| 항목 | 내용 |
|:---|:---|
| **규칙명** | 캐시 키 형식 통일 |
| **도메인** | 캐싱 |
| **조건 (When/If)** | Google Sheets `_TICKER_CACHE_` 탭에 캐시를 저장하거나 조회 시 |
| **행동 (Then)** | 키를 `{code}_{section}` 형식으로 생성. 예: `005930_price`, `005930_dart` |
| **우선순위** | 중간 |
| **상태** | 확정 |
| **연관** | DR-002 |

#### BR-012 · 강제 갱신 시 캐시 무효화

| 항목 | 내용 |
|:---|:---|
| **규칙명** | 강제 갱신 시 캐시 무효화 |
| **도메인** | 캐싱 |
| **조건 (When/If)** | 사용자가 종목 상세 페이지의 데이터 갱신 버튼을 클릭 시 |
| **행동 (Then)** | TTL 만료 여부와 관계없이 해당 종목의 `_TICKER_CACHE_` 전 섹션을 즉시 무효화, 전체 재조회 후 저장 |
| **우선순위** | 중간 |
| **상태** | 확정 |
| **연관** | FR-022 |

---

### 데이터 처리 도메인

#### BR-013 · 매매 유형 제한

| 항목 | 내용 |
|:---|:---|
| **규칙명** | 매매 유형 제한 |
| **도메인** | 데이터 처리 |
| **조건 (When/If)** | 매매내역 시트의 Type 컬럼 데이터를 처리 시 |
| **행동 (Then)** | `매수` 또는 `매도` 값만 유효 처리. 이외 값은 무효 행으로 처리하여 집계에서 제외 |
| **우선순위** | 높음 |
| **상태** | 확정 |
| **연관** | DR-001 |

#### BR-014 · RSI·MACD 클라이언트 계산

| 항목 | 내용 |
|:---|:---|
| **규칙명** | RSI·MACD 클라이언트 계산 |
| **도메인** | 데이터 처리 |
| **조건 (When/If)** | 종목 상세 보조지표 섹션 렌더링 시 |
| **행동 (Then)** | KIS `dailyPrice` 데이터(서버에서 수신)를 기반으로 RSI(14), MACD(12,26,9)를 클라이언트 사이드에서 계산. 별도 외부 지표 API 호출 없음 |
| **우선순위** | 중간 |
| **상태** | 확정 |
| **연관** | FR-019 |

#### BR-015 · Vercel 서버리스 번들 크기 제한

| 항목 | 내용 |
|:---|:---|
| **규칙명** | Vercel 서버리스 번들 크기 제한 |
| **도메인** | 데이터 처리 |
| **조건 (When/If)** | Vercel 배포 빌드 수행 시 |
| **행동 (Then)** | 서버리스 함수 번들이 250MB를 초과하지 않도록 `next.config.mjs`의 `outputFileTracingExcludes`에 무거운 의존성 제외 설정 |
| **우선순위** | 높음 |
| **상태** | 확정 |
| **연관** | CR-001 |

#### BR-016 · 통합 갱신 함수 타임아웃 설정

| 항목 | 내용 |
|:---|:---|
| **규칙명** | 통합 갱신 함수 타임아웃 설정 |
| **도메인** | 데이터 처리 |
| **조건 (When/If)** | `/api/ticker/[code]/refresh` 엔드포인트 실행 시 |
| **행동 (Then)** | `export const maxDuration = 60` 설정으로 함수 타임아웃 60초 확보. Vercel Pro Plan 필수 |
| **우선순위** | 낮음 |
| **상태** | 확정 |
| **연관** | CR-005, FR-022 |

---

### 전체 BR 테이블

| BR-ID | 규칙명 | 도메인 | 조건 (When/If) | 행동 (Then) | 우선순위 | 상태 |
|:---|:---|:---:|:---|:---|:---:|:---:|
| BR-001 | 이메일 허용 검증 | 인증 | ALLOWED_EMAIL 설정 시 로그인 시도 | 등록 이메일만 허용, 미등록 시 401 | 높음 | 확정 |
| BR-002 | JWT 세션 전역 보호 | 인증 | 모든 페이지·API 접근 시 | middleware.ts 전역 검증, 미인증 시 리다이렉트 | 높음 | 확정 |
| BR-003 | 세션 만료 UI 처리 | 인증 | API 응답 401 발생 시 | 강제 리다이렉트 대신 UI 프롬프트 표시 | 중간 | 확정 |
| BR-004 | API 키 서버 전용 격리 | 인증 | 컴포넌트 빌드 시 | KIS·DART·OpenAI·Google 키 클라이언트 노출 금지 | 높음 | 확정 |
| BR-005 | API 호출 스로틀 준수 | KIS API 제약 | KIS API 연속 호출 시 | 최소 400ms 대기 (KIS_THROTTLE_MS) | 높음 | 확정 |
| BR-006 | 토큰 단일 발급 원칙 | KIS API 제약 | 신규 토큰 발급 요청 시 | 유효 토큰 존재 시 재사용, 만료 시만 재발급 | 높음 | 확정 |
| BR-007 | 토큰 발급 레이스 컨디션 방지 | KIS API 제약 | 동시 토큰 요청 시 | softExpireKisToken()으로 중복 발급 차단 | 높음 | 확정 |
| BR-008 | 토큰 다계층 캐시 조회 | KIS API 제약 | 토큰 조회 시 | globalThis → /tmp/ 순서 조회, 없으면 발급 후 양쪽 저장 | 높음 | 확정 |
| BR-009 | KIS/DART 캐시 TTL 30분 | 캐싱 | _TICKER_CACHE_ 조회 시 | 30분 초과 항목 만료, 재호출 후 갱신 | 높음 | 확정 |
| BR-010 | AI 분석 캐시 TTL 7일 | 캐싱 | _AI_CACHE_ 조회 시 | 7일 초과 항목 만료, OpenAI 재호출 | 높음 | 확정 |
| BR-011 | 캐시 키 형식 통일 | 캐싱 | Sheets 캐시 저장·조회 시 | {code}_{section} 형식 키 생성 | 중간 | 확정 |
| BR-012 | 강제 갱신 시 캐시 무효화 | 캐싱 | 갱신 버튼 클릭 시 | TTL 미만이어도 전 섹션 무효화 후 재조회 | 중간 | 확정 |
| BR-013 | 매매 유형 제한 | 데이터 처리 | 매매내역 Type 컬럼 처리 시 | 매수/매도 이외 값 무효, 집계 제외 | 높음 | 확정 |
| BR-014 | RSI·MACD 클라이언트 계산 | 데이터 처리 | 보조지표 섹션 렌더링 시 | KIS dailyPrice 기반 클라이언트 계산, 외부 API 미사용 | 중간 | 확정 |
| BR-015 | Vercel 번들 크기 제한 | 데이터 처리 | 배포 빌드 시 | 서버리스 함수 250MB 이하 유지 | 높음 | 확정 |
| BR-016 | 통합 갱신 함수 타임아웃 | 데이터 처리 | /api/ticker/[code]/refresh 실행 시 | maxDuration=60 설정, Vercel Pro 필수 | 낮음 | 확정 |

---

## 3. 규칙 간 연계

| BR-ID | 선행 규칙 | 관계 | 비고 |
|:---|:---|:---|:---|
| BR-002 | BR-001 | 순차 적용 | ALLOWED_EMAIL 검증(001) 후 JWT 세션 발급(002) |
| BR-003 | BR-002 | 예외 처리 | JWT 검증 실패(002) 시 UI 처리(003) 적용 |
| BR-006 | BR-008 | 포함 | 토큰 조회(008) 결과에 따라 발급 여부 결정(006) |
| BR-007 | BR-006 | 선행 | 발급 시도(006) 전 레이스 컨디션 방지(007) 선행 |
| BR-012 | BR-009 | 우선순위 | 강제 갱신(012)은 TTL 규칙(009)보다 우선 적용 |
| BR-012 | BR-010 | 우선순위 | 강제 갱신(012)은 AI TTL 규칙(010)에도 동일 적용 |

---

## 문서 버전 이력

| 버전 | 일자 | 작성자 | 변경 내용 |
|:---|:---|:---|:---|
| v0.1 | 2026-06-07 | Claude Sonnet 4.6 | 최초 생성 (16개 규칙, 4개 도메인) |
